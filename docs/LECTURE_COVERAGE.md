# 강의 핵심 주제 대비 NaN-SE 반영 점검

기말 범위(UI 설계·코딩·테스팅·유지보수·품질)와 중간 이전 원리(요구·설계원리·SOLID·응집도/결합도·아키텍처·관리)에서 강조된 SW공학 표준 개념을 NaN-SE가 어디까지 반영했는지 정직하게 매핑한다. 과대포장하지 않고 검출 구현 / 설명층 / 문서·정책 / 범위 밖을 구분한다.

상태 표기:
- **검출**: 결정론적 정적 분석으로 도구가 직접 측정
- **설명**: 검출된 위반을 LLM 학습 카드가 설명
- **문서/정책**: 코드가 아니라 설계 문서·정책으로 반영
- **범위 밖**: 의도적으로 구현하지 않음(사유 명시)

## 1. 코딩 원리 (SOLID·응집도·결합도)

| 주제 | NaN-SE 반영 | 상태 |
|---|---|---|
| 단일 책임(SRP) | LCOM4를 직접 구현해 클래스 응집 결손을 정량 검출. 임계 초과 시 SRP finding | 검출 |
| 응집도 (기능적~우연적) | LCOM4 연결 요소 개수로 응집 분리 신호를 제공. 7단계 명칭 분류까지는 아님 | 검출(부분) |
| 결합도 (자료~내용) | 미구현. 결합도 정량화(Ca/Ce, Instability)는 설계만 언급 | 범위 밖 |
| 개방-폐쇄(OCP) | 결정론적 검출 안 함. `examples/income_tax_solid.py`로 개념 예시 + 학습 카드 설명 대상 | 설명/문서 |
| 의존 역전(DIP) | 위와 같음. 단 NaN-SE 자신은 LLM 호출을 주입 가능한 `complete`로 분리해 DIP를 코드로 실천 | 설명/문서 |
| 리스코프(LSP)·인터페이스 분리(ISP) | 기계적 판정이 어려워 검출 안 함. 보고서에서 한계로 명시 | 범위 밖 |

OCP/DIP/LSP/ISP를 LLM에 채점시키지 않는 건 의도적 결정이다. 확률적 채점은 신뢰할 수 없다는 판단("Are We SOLID Yet?" 결과)에서 검출은 결정론적 메트릭으로 가능한 SRP/복잡도에 한정하고, 나머지는 설명층과 문서로 다룬다. 경위는 DISCUSSION_LOG.md Day 5.

## 2. 테스팅·복잡도

| 주제 | NaN-SE 반영 | 상태 |
|---|---|---|
| 순환복잡도(사이클로메틱) | radon `cc_visit`로 메서드별 검출. 임계 10 초과를 finding으로 | 검출 |
| 복잡도와 시험 용이성의 관계 | 복잡도가 높으면 경로가 늘어 완전한 테스트가 어렵다는 점을 finding 심각도로 환기. `examples/order_router.py`가 예시 | 검출/문서 |
| 검증(Verification)과 확인(Validation) 구분 | "검수는 사람" 정책의 근간. 도구는 검출·설명만, 최종 판단은 사용자 | 정책 |
| 테스트 케이스·스크립트의 존재 | pytest 33개로 검출·추적·통합 흐름을 회귀 검증 | 문서/구현 |
| CI/CD·컨테이너화 | Dockerfile·docker-compose로 재현 가능한 빌드·실행(자동 파이프라인은 미구현, REPORT 6.4) | 구현(부분) |
| QA의 독립성·자기 의심 | 1차 결과를 의심해 재검토한 메타 V&V를 의사결정 기록에 남김. 학습 카드 거절 사유 반영 루프 | 정책 |

## 3. 품질 (ISO 25010)

| 주제 | NaN-SE 반영 | 상태 |
|---|---|---|
| 품질은 프로세스(요구)부터 결정 | NaN-SE의 전제 자체. 코드 작성 단계에 공학 절차를 끼워넣음 | 정책 |
| 유지보수성 하위특성(모듈성·시험성·분석성·수정성) | LCOM4(모듈성)·순환복잡도(시험성·분석성)가 직접 닿는 지표 | 검출 |
| ISO 25010 8특성 전체 매핑 | Process Log 모듈로 설계만. 미구현 | 문서 |

## 4. 요구·설계·관리·아키텍처

| 주제 | NaN-SE 반영 | 상태 |
|---|---|---|
| 유스케이스 다이어그램(include·extend, 점선·화살표) | REQUIREMENTS.md에 Mermaid로 작성, 관계 정당화 | 문서 |
| 동사=기능 / 형용사=비기능 요구 구분 | REQUIREMENTS 비기능 요구 절에 반영 | 문서 |
| 단계별 진입 조건·보상 트랜잭션 | 설계 문서에서 개념만 언급. SDLC 단계에 보상 트랜잭션을 비유한 것이라 분산 트랜잭션 SAGA의 직접 적용은 아니다. Stage 코드 없음 | 문서(개념) |
| WBS·EV(SPI/CPI)·여유시간·브룩스 법칙 | 이 과제 자체의 일정 관리에 WBS.md 일정·EV 손계산·Brooks 위험으로 반영. 도구 기능은 아님 | 문서 |
| 아키텍처 패턴(전략·choreography) | 전략 패턴은 OCP 예시(`examples/`)로 설명. choreography 이벤트 버스는 미구현 확장 방향 | 문서 |

## 5. 정직한 갭

1. **결정론적 검출은 SRP(LCOM4)와 순환복잡도 둘뿐이다.** 결합도·OCP·DIP·LSP·ISP는 검출하지 않는다. 이건 한계가 아니라 "확률적 채점을 신뢰하지 않는다"는 설계 결정의 결과다. 보고서에 분명히 적는다.
2. **피벗 전 서술은 정리했다(Day 5 후속).** ARCHITECTURE.md·VISION.md·LEARNING_CARDS.md·METRICS.md·AI_TOOLING.md 상단에 피벗 배너를 달고, 옛 "SOLID Judge(LLM 채점)" 표현을 검출층(Metric Analyzer)과 LLM 설명층(Learning Card)으로 정정했다. DISCUSSION_LOG·REQUIREMENTS·WBS의 "SOLID Judge"는 피벗 경위를 기록하는 역사 서술이라 의도적으로 남겼다.
3. **응집도 7단계·결합도 6단계의 명칭 분류는 안 한다.** LCOM4는 연결 요소 개수라 "분리가 필요하다"는 신호는 주지만 "이건 교환적 응집"처럼 단계명을 붙이지는 않는다.

## 6. 강의가 강조한 메시지와 NaN-SE의 정렬

강의에서 반복된 핵심은 컴퓨팅의 본질이 프로세스라는 것, 품질은 요구 단계부터 시작한다는 것, 검증과 확인을 정확히 구분하라는 것, 그리고 개발자는 자기 코드를 의심하고 꼼꼼히 검증하라는 것이다. NaN-SE는 이 메시지를 도구로 옮긴 시도다. 코드 작성 시점에 공학 메트릭을 끼워넣고(프로세스), 위반을 학습 기회로 바꾸되 최종 판단은 사람에게 남긴다(V&V). 도구가 사람의 검증을 대체한다고 주장하지 않는 게 핵심이고, 이게 강의의 "검수는 사람" 메시지와 같은 자리에 선다.
